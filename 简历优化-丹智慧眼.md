# 丹智慧眼 — 简历优化（基于真实代码核实版）

> 核实依据：`face_geometry.py` / `recommend_rules.py` / `model_api.py` / `backend/backend_main.py` / `model_train.py` / `tests/` / `.github/workflows/ci.yml` / `requirements.txt` 全部源码逐行核对。技术面可经得起追问。

---

## 〇、原简历 vs 真实代码 — 事实核对（内部参考，勿写入简历）

| 原简历声明 | 真实代码情况 | 处置 |
|---|---|---|
| 基于 MTCNN 实现人脸检测与对齐 | MTCNN 仅作 **降级兜底**（`face_geometry.py` `_get_mtcnn()`），主路径是 MediaPipe FaceMesh 468 关键点 | 改写为 MediaPipe 主路径 + MTCNN/Haar 多级降级 |
| 独立设计、训练 2 层 CNN 用于 4 种脸型识别 | `model_train.py` 第 65-70 行明确：**数据目录为空时生成 10 张随机像素图当训练数据**，模型完全不可用，`model_api.py` 注释已标注"由随机噪声训练的 CNN 更换为 MediaPipe 几何方案""不再加载 .pth/.pkl 假模型文件" | 删除 CNN 训练声明，改为 MediaPipe 几何规则分类（零训练数据依赖） |
| CPU/GPU 环境下的快速推理与部署 | `model_train.py` 第 101 行 `device = torch.device("cpu")` 写死 CPU，无 GPU 路径 | 删除 GPU 推理声明，改为 CPU 端实时几何分析 |
| 决策树分类器构建个性化推荐模型 | `model_train.py` 第 148-163 行：决策树 **标签是 if-else 拍脑袋生成的伪标签**（"高度近视推荐圆框""瞳距大推荐方框""其他情况 np.random.randint"），非真实监督学习 | 改为透明规则推荐引擎（脸型映射+度数硬过滤+折射率/瞳距加权打分） |
| Top-3 命中率随数据积累持续提升 | 无任何命中率指标采集、无 A/B 测试、无在线学习回路 | 删除该声明，改为"逐款返回命中规则与分值，可解释可调参" |
| 数据采集爬虫 → 标注 → 用户行为记录的数据闭环 | `tools/眼镜数据爬取代码.py` 注释自标"此处为示例"，未实际跑通；真实数据来源是 **Wikimedia Commons CC 图片 + `tools/import_real_glasses.py` 灵活列映射导入工具**；用户行为仅落"推荐记录"表，无标注/回流闭环 | 改为真实数据管线：Wikimedia Commons 抓取 + 列映射归一导入工具 + 推荐记录落库 |

**结论**：原简历 4 条声明里有 3.5 条与代码不符，技术面一追问即露馅。本优化版全部改为真实代码口径，且每条都能在源码中找到对应实现。

---

## 一、主简历条目（精炼版 · 可直接投递）

**丹智慧眼 ｜ 基于 AI 的眼镜智能推荐平台 ｜ 大创省级项目 · 核心开发 ｜ 2025.09 – 2025.12**

- **项目定位**：主导算法与工程落地，构建"拍照 → 脸型识别 → 智能推荐 → 选购"全闭环，面向"中国眼镜之都"丹阳眼镜零售选型场景，解决传统推荐不精准、导购说不清的痛点。
- **AI 脸型识别**：基于 **MediaPipe FaceMesh 468 个面部关键点**提取 9 项几何指标（脸长宽比 / 下颌宽比 / 额头宽比 / 下颌角 / 三庭五眼比例等），用自研阈值规则将脸型分为长方形 / 圆形 / 方形 / 鹅蛋脸 4 类，**零训练数据依赖、判定可解释**；MediaPipe 不可用时降级至 MTCNN / OpenCV Haar 检测框粗分类，主流程可用性 100%。
- **可解释推荐引擎**：自研透明规则引擎——"脸型→镜框映射 +40 分 + 近视度数硬过滤 + 折射率加分（高度近视 1.67/1.74 优先 +20）+ 瞳距匹配加分（≤2mm +10）"，输出 **Top-3 推荐且逐款返回命中规则与分值**；兼容屈光度（-3.00）与"度数"（300 自动归一）双输入、缺数据自动兜底，破解传统推荐"黑盒不可信"。
- **三层解耦架构**：前端（HTML/JS）+ Flask 后端（SQLAlchemy + SQLite）+ FastAPI 模型服务独立部署，模型可独立升级；落地 **JWT 鉴权、flask-limiter 限流（120/min 默认 + 10/min 敏感接口）、安全响应头（CSP/X-Frame-Options 等）、ProxyFix 真实 IP、loguru 按天轮转日志**。
- **工程化交付**：**Docker 三服务编排 + GitHub Actions CI（pytest + Docker 镜像构建双阶段）**，**94 个 pytest 用例全绿**（含参数化展开）；人脸图片仅在请求生命周期内做几何分析、**不落库生物特征**，降低《个人信息保护法》合规负担。
- **技术栈**：Python · FastAPI · Flask · MediaPipe · OpenCV · SQLAlchemy · SQLite · PyJWT · Docker · Nginx · GitHub Actions

---

## 二、三套岗位差异化版本（按投递方向选用）

### 🅰 版本 A · Python 后端方向（突出 Web 服务 / 数据库 / 工程化）

**丹智慧眼 ｜ Flask + FastAPI 微服务架构的眼镜智能推荐平台 ｜ 大创省级项目 · 核心开发 ｜ 2025.09 – 2025.12**

- 主导**前后端分离 + 模型服务独立部署的三层架构**设计：Flask 后端（SQLAlchemy + SQLite）负责鉴权 / 商城 / 购物车 / 管理后台，FastAPI 模型服务负责 CV 推理与推荐，两者通过 REST API 解耦，模型可独立升级不影响业务层。
- 落地**生产级 Web 工程实践**：JWT 注册登录（24h token + admin 角色权限）、flask-limiter 接口限流（默认 120/min + 登录注册 10/min）、CSP / X-Frame-Options 等安全响应头、ProxyFix 反代取真实 IP、loguru 结构化日志按天轮转 + 慢请求告警、pydantic-settings 环境变量配置化。
- 设计 **SQLAlchemy 数据模型**（Account / User / Glasses / RecommendRecord 四表），实现商品 CRUD、CSV 批量导入、分页筛选搜索、推荐记录关联账号；后端通过 `requests` 转发模型服务并统一 JSON 错误格式（404/405/429/500）。
- **工程化交付**：Docker 三服务编排（含 PostgreSQL prod profile）、Nginx 反代配置、GitHub Actions CI（pytest + Docker 镜像构建双阶段）、**94 个 pytest 用例全绿**（覆盖认证 / 限流 / 安全头 / 推荐 / 几何分类 / 模型 API）。
- **技术栈**：Python · Flask · FastAPI · SQLAlchemy · SQLite · PyJWT · flask-limiter · loguru · Docker · Nginx · GitHub Actions · pytest

### 🅱 版本 B · AI 应用方向（突出 CV / 推荐算法 / 可解释 AI）

**丹智慧眼 ｜ 基于 MediaPipe 几何 CV 与可解释推荐的眼镜智能选购平台 ｜ 大创省级项目 · 核心开发 ｜ 2025.09 – 2025.12**

- 针对学生项目缺标注数据、无 GPU 的约束，**弃用 CNN 训练方案**，设计基于 **MediaPipe FaceMesh 468 个面部关键点**的几何规则脸型分类器：提取脸长宽比 / 下颌宽比 / 额头宽比 / 下颌角 / 三庭五眼比例等 9 项指标，用自研阈值规则分类 4 种脸型，**零训练数据依赖、判定可追溯**。
- 设计**多级降级容错**：MediaPipe 不可用 → MTCNN 检测框长宽比粗分类 → OpenCV Haar 兜底 → 默认脸型，每一级在响应中标注 `method` 字段，**主流程可用性 100%**；并解决 MediaPipe 在 Windows 中文路径下的 C++ ANSI 解析坑（用 ASCII 目录联接规避）。
- 自研**可解释规则推荐引擎**：脸型→镜框映射（+40）+ 度数硬过滤 + 折射率加权（高度近视 1.67/1.74 优先 +20 / 中度近视 ≥1.60 +10）+ 瞳距匹配（≤2mm +10 / ≤4mm +5），输出 Top-3 并**逐款返回命中规则与分值**，用户与导购都能看懂"为什么推荐这副眼镜"。
- 输入兼容工程化：近视度数同时接受屈光度（-3.00）与"度数"（300 自动除 100 转 -3.00）、0 度跳过过滤、库存无匹配时自动放宽脸型限制补满，**避免空结果破坏体验**。
- 将模型服务封装为 **FastAPI 微服务**（健康检查 / 耗时中间件 / pydantic 入参校验 / loguru 结构化日志），前端通过后端转发调用，人脸图片**仅在请求内几何分析、不落库生物特征**。
- **技术栈**：Python · MediaPipe · OpenCV · FastAPI · pydantic · pandas · numpy · pytest

### 🅲 版本 C · 运维 / DevOps 方向（突出容器化 / CI/CD / 可观测性 / 部署）

**丹智慧眼 ｜ Docker 化部署 + CI/CD 的 AI 眼镜推荐平台 ｜ 大创省级项目 · 核心开发 ｜ 2025.09 – 2025.12**

- 设计**三服务 Docker 编排**（前端 / Flask 后端 / FastAPI 模型服务），编写 `Dockerfile.backend` / `Dockerfile.model` / `Dockerfile.frontend` 与 `docker-compose.yml`，支持 `--profile prod` 一键切换 SQLite → PostgreSQL；提供 Nginx 反代配置参考。
- 搭建 **GitHub Actions CI 双阶段流水线**：`test` job 跑 pytest 全量用例 → `docker` job 依赖 test 成功后构建三服务镜像，**push/PR 自动触发**，主分支 94 用例全绿才允许构建镜像。
- 落地**可观测性与稳定性实践**：loguru 按天轮转日志（保留 30 天）+ stdout 同步输出、FastAPI `X-Process-Time` 响应头记录请求耗时、>2s 慢请求自动 warning、`/health` 健康检查接口（含 mediapipe 可用性 + 商品库数量）。
- 落地**生产级安全加固**：flask-limiter 接口限流（默认 120/min + 登录注册 10/min）、CSP / X-Frame-Options / X-Content-Type-Options / Referrer-Policy 安全响应头、ProxyFix 反代取真实 IP、JWT 鉴权、admin 角色权限、SECRET_KEY / ADMIN_PASSWORD 环境变量化（`.env.example` 模板）。
- 编写 **94 个 pytest 用例**（认证 8 / 管理后台 14 / 商品 18 / 提交 10 / 几何分类 6 / 规则推荐 14 / 模型 API 9，含参数化展开），覆盖核心业务路径与边界条件；conftest 隔离测试数据库 + 重置限流存储，避免跨用例污染。
- **技术栈**：Docker · docker-compose · GitHub Actions · Nginx · Flask · FastAPI · loguru · pytest · pydantic-settings · ProxyFix

---

## 三、项目亮点（用于自我介绍 / 简历"项目亮点"栏，挑 3 条）

1. **透明 AI 差异化**：在精度与可解释性之间优先可解释，推荐逐条可追溯规则与分值——契合导购"说得清"诉求，是相较同类作品的核心壁垒。
2. **零数据壁垒的鲁棒 CV**：弃用噪声训练的 CNN，改用 MediaPipe 几何规则 + 多级降级，无标注 / 无 GPU 即可运行，对遮挡 / 大表情 / 侧脸优雅降级。
3. **生产级工程素养**：微服务解耦、Docker 编排、CI/CD、94 测试、鉴权 / 限流 / 安全头 / 日志齐全，远超一般课程 demo 的"单脚本跑通"水准。
4. **隐私合规设计**：人脸不落库，降低《个人信息保护法》对生物识别信息的合规负担，体现工程责任感。
5. **真实产业锚定**：面向"中国眼镜之都"丹阳零售选型，兼顾技术深度与现场可演示性，非玩具项目。

---

## 四、⚠️ 面试防追问红线（内部参考，勿写入简历）

以下问题在技术面**极高概率被问到**，请按真实代码口径作答，切勿沿用原简历措辞：

| 面试官可能问 | ❌ 不能这么说 | ✅ 真实口径应答 |
|---|---|---|
| 你的 CNN 怎么训练的？数据集多大？ | "自训 2 层 CNN，xxx 张脸型图片" | "项目早期尝试过 CNN 路线，但学生项目缺标注数据，我用随机噪声跑通了训练流程作为占位，后来评估不可用就**主动弃用**，换成 MediaPipe 几何规则——这个决策本身也是工程取舍的体现" |
| 为什么不用 CNN？精度差多少？ | "CNN 精度更高但训练慢" | "几何规则在 4 类脸型分类上够用且**完全可解释**，导购场景需要'说清为什么'，可解释性比 1-2 个点的精度更重要；且无标注数据 / 无 GPU 时几何方案零门槛启动" |
| 推荐命中率多少？怎么提升的？ | "随数据积累命中率持续提升" | "推荐是规则引擎不是学习模型，没有'命中率提升曲线'；评估方式是**规则覆盖率与可解释性**——每条推荐都返回命中规则与分值，可人工审计调参" |
| 爬虫怎么做的？反爬怎么处理？ | "爬拼多多 + BeautifulSoup" | "拼多多页面是动态渲染，简单 requests 抓不到；真实数据来源是 **Wikimedia Commons 的 CC 协议眼镜图 + `import_real_glasses.py` 灵活列映射导入工具**，把外部 CSV/Excel 归一为标准 schema" |
| GPU 推理怎么部署的？ | "CPU/GPU 双环境" | "纯 CPU 推理，MediaPipe FaceMesh 在 CPU 上单次推理几十毫秒，没必要上 GPU；如要扩展到视频流场景再考虑 GPU" |
| 数据闭环怎么做的？ | "爬虫→标注→用户行为→模型迭代" | "用户提交推荐后会落 `RecommendRecord` 表（含脸型 / 参数 / 推荐结果），**为后续协同过滤或规则调优积累数据**；当前还没有自动回流迭代，属于后续展望" |

**核心原则**：宁可说"这个我没做 / 是后续展望"，也不要把没做的说成做了。面试官追问细节时，"主动弃用不可用方案"比"训练了一个能用的模型"更可信、也更显工程判断力。

---

## 五、开源地址

https://github.com/liwe123/Online-Eyewear-Platform
