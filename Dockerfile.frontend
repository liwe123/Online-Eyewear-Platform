FROM nginx:alpine

# 静态前端
COPY frontend/ /usr/share/nginx/html/
# 自定义 nginx 配置（安全头 / gzip / 限流 / 上传限制），覆盖默认 server 片段
COPY nginx/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
